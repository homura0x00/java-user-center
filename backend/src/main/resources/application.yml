server:
  port: 9000
  servlet:
    context-path: /api
spring:
  application:
    name: backend
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yuetu_web
    username: root
    password: 12345678

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false